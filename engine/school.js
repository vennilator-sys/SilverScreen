const School = {

    advanceYear(player) {

        const result = {

            history: []

        };

        if(player.age < 5)
            return result;

        // grade progression

        // GPA

        // reputation

        return result;

    }

};
